PORT=3000
NODE_ENV=development
# JWT secret for authentication. A default will be seeded in database if empty.
JWT_SECRET=sacp-super-secret-key-123
# Google Drive Folder ID and JSON Credentials (can be configured in Settings UI instead)
GDRIVE_FOLDER_ID=
GDRIVE_CREDENTIALS=
